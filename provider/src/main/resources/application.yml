spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: rootroot
    url: jdbc:mysql://127.0.0.1:3306/trail?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    max-active: 20
    max-wait: 5000
    initial-size: 1
    filters: stat,log4j,wall
    validation-query: SELECT 'X'   #验证连接
    enable: true